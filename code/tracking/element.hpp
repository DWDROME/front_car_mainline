#pragma once

#include "types.hpp"

//----------------------------------------------------------------------------------------------------------------------
//  @brief      赛道元素互斥处理入口
//  @param      rt   运行时状态；读取 track/cross/ring，更新 cross 或 ring 状态
//  @return     int  1 当前有十字或环岛状态在处理 / 0 当前没有元素状态
//  @note       十字优先级高于环岛；十字仍在时清 ring，空闲或十字退出后再尝试 ring。zebra 放在主线输出后单独处理。
//----------------------------------------------------------------------------------------------------------------------
int element_process(runtime_t *rt);
