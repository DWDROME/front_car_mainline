#pragma once

#include "types.hpp"

//----------------------------------------------------------------------------------------------------------------------
//  @brief      斑马线和底部停车线检测入口
//  @param      rt        运行时状态；读取 gray 和动态阈值，更新 rt->zebra
//  @param      scan_mid  斑马线扫描用中线；为空时只更新底部停车线
//  @note       zebra.detected 表示中线两侧检测到连续黑白段；stop_line 表示底部黑线连续多帧确认。
//              scan_mid 由 mainline 按参考 garage 语义选择，不一定等于当前控制中线。
//----------------------------------------------------------------------------------------------------------------------
void zebra_process(runtime_t *rt, const midline_t *scan_mid);
