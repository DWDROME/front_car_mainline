#pragma once

#include "types.hpp"

//----------------------------------------------------------------------------------------------------------------------
//  @brief      环岛状态机入口
//  @param      rt  运行时状态；读取左右边界 L 角、直线判断、编码器累计值，更新 rt->ring 和必要的边界点
//  @note       kind 表示左/右环；state 按 BEGIN -> IN -> RUN -> OUT -> END 推进。
//              cross 已活动时不进入新环岛，元素互斥顺序由 element_process() 统一控制。
//----------------------------------------------------------------------------------------------------------------------
void ring_process(runtime_t *rt);
