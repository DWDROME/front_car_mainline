#pragma once

#include "types.hpp"

//----------------------------------------------------------------------------------------------------------------------
//  @brief      十字状态机入口
//  @param      rt  运行时状态；读取左右边界 L 角、trace、IPM 矩阵，更新 rt->cross 和必要的边界点
//  @note       状态语义：NONE 判双 L，BEGIN 截近线，IN 构建远线并等待近线丢失后恢复。
//              ring 已活动时由 element_process() 保证互斥，本函数只维护 cross 自身状态。
//----------------------------------------------------------------------------------------------------------------------
void cross_process(runtime_t *rt);
