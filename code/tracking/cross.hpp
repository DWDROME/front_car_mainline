#pragma once

#include "types.hpp"

//----------------------------------------------------------------------------------------------------------------------
//  @brief      十字状态机入口
//  @param      rt  运行时状态；读取 strict double-L 复核、近线、trace、IPM 矩阵，更新 rt->cross
//  @note       状态语义：NONE 只接受 l_pair_ok strict 入口，BEGIN 截近线，IN 构建远线并等待近线丢失后恢复。
//              ring 已活动时由 element_process() 保证互斥，本函数只维护 cross 自身状态。
//----------------------------------------------------------------------------------------------------------------------
void cross_process(runtime_t *rt);
