# 控制参数笔记

这份笔记记录 `front_car_mainline` 当前控制参数的位置、加载顺序和每帧调用关系。这里说的是当前真实主链，不记录旧的环境变量调参旁路。

## 1. 参数来源顺序

控制参数只有这一条来源链：

```text
code/core/config.hpp 默认值
-> 启动时 load_control_config(path) 读取外部配置覆盖
-> control.cpp 每帧通过 control_config() 取当前参数
-> solve_runtime_with_feedback(rt, fb, ctrl) 计算 duty
-> drive_output_apply(ctrl) 输出到电机
```

外部配置路径由 `code/app/main.cpp` 决定：

```text
FRONT_CAR_CONFIG 环境变量非空：使用 FRONT_CAR_CONFIG 指定的路径
否则：使用 /root/front_car_mainline.yaml
```

入口代码位置：

```text
code/app/main.cpp
-> resolve_config_path()
-> load_control_config(resolve_config_path())
```

## 2. 参数文件位置

默认值：

```text
code/core/config.hpp
```

本地示例配置：

```text
front_car_mainline.yaml
```

板端默认配置：

```text
/root/front_car_mainline.yaml
```

可用环境变量指定其他路径：

```bash
FRONT_CAR_CONFIG=/path/to/front_car_mainline.yaml ./front_car_mainline
```

如果外部配置文件不存在，会打印警告并继续使用 `config.hpp` 默认值：

```text
ConfigWarn: <path> not found, using config.hpp defaults
```

## 3. 配置格式

解析代码在：

```text
code/core/config.cpp
```

当前解析器只支持简单的 `key: value`，不是完整 YAML 解析器。

支持：

```yaml
target_rps: 4.0
outer_kp: 0.03
left_speed_kp: 1.25
```

支持 `#` 注释：

```yaml
outer_kd: 0.0  # 图像误差抖动时不要轻易打开
```

不支持嵌套：

```yaml
control:
  target_rps: 4.0
```

未知 key 会打印：

```text
ConfigWarn: unknown key '<key>'
```

## 4. 当前支持的 key

这些 key 来自 `code/core/config.cpp::set_field()`，字段名必须完全一致。

```text
control_period_ms

target_rps
element_target_rps
speed_target_rps

left_speed_base_percent
right_speed_base_percent
left_speed_kp
right_speed_kp
left_speed_ki
right_speed_ki

wheel_track_m
encoder_gear_diameter_m

gyro_raw_to_rad_s
imu_yaw_feedback_enabled

outer_kp
outer_kd
outer_sign
straight_error_threshold
straight_turn_scale
max_target_yaw_rate

yaw_kp
yaw_ki
max_yaw_rate_correction

rps_filter_alpha
yaw_rate_filter_window

max_duty_percent
```

## 5. 每帧如何调用

实车闭环主入口是：

```text
solve_runtime_with_feedback(rt, fb, ctrl)
```

典型每帧顺序：

```text
tracking_process_frame(rt)
-> drive_output_read_feedback(&fb)
-> solve_runtime_with_feedback(rt, &fb, &ctrl)
-> drive_output_apply(&ctrl)
```

`solve_runtime_with_feedback()` 内部进入 `control.cpp::solve()`，然后读取当前参数：

```cpp
const control_config_t &c = control_config();
```

之后整帧都通过 `c.xxx` 使用参数。

无反馈/离线入口是：

```text
solve_runtime(rt, ctrl)
```

它会构造空的 `control_feedback_t`，主要用于无编码器和无 IMU 反馈的路径；实车闭环优先看 `solve_runtime_with_feedback()`。

## 6. 参数对应的控制阶段

### 6.1 目标速度

```text
target_rps：普通巡线中心目标速度
element_target_rps：十字/环岛内中心目标速度
speed_target_rps：左右轮 FF 前馈缩放参考
```

使用位置：

```text
control.cpp
-> center_rps = target_rps / element_target_rps
-> FF 用 speed_target_rps 做缩放参考
```

### 6.2 视觉外环

```text
outer_kp
outer_kd
outer_sign
straight_error_threshold
straight_turn_scale
max_target_yaw_rate
```

控制链：

```text
guide_error
-> outer_kp * err + outer_kd * derr
-> outer_sign
-> 直道小误差缩放
-> max_target_yaw_rate 限幅
-> target_yaw
```

注意：`target_yaw` 是视觉外环给出的目标角速度，不是最终 duty。

### 6.3 yaw-rate 内环

```text
imu_yaw_feedback_enabled
max_yaw_rate_correction
yaw_rate_filter_window
```

控制链：

```text
actual_yaw_rate_mrad_s
-> yaw_avg()
-> yaw_now
-> target_yaw - yaw_now
-> yaw PI
-> max_yaw_rate_correction 限幅
-> yaw_cmd
```

如果 IMU 反馈无效、`imu_yaw_feedback_enabled == 0`，或者 `max_yaw_rate_correction <= 0`，则本帧直接使用：

```text
yaw_cmd = target_yaw
```

### 6.4 差速运动学

```text
wheel_track_m
encoder_gear_diameter_m
```

控制链：

```text
center_rps + yaw_cmd
-> center_mps / diff_mps
-> target_l / target_r
```

注意：轮距和编码轮直径不准，会直接影响同一个 `yaw_cmd` 下的左右轮目标速度差。

### 6.5 左右轮 FF + PI

```text
left_speed_base_percent
right_speed_base_percent
left_speed_kp
right_speed_kp
left_speed_ki
right_speed_ki
rps_filter_alpha
max_duty_percent
```

控制链：

```text
encoder count
-> count_to_rps()
-> rps_filter_alpha 低通
-> g_left_rps / g_right_rps
-> target_l/r - g_left/right_rps
-> base_percent FF + speed PI
-> duty_l / duty_r
-> max_duty_percent 限幅
```

注意：`drive_output.cpp` 里仍有硬件安全上限，控制层 `max_duty_percent` 不是唯一限幅。

## 7. 最短记忆版

```text
参数默认值：code/core/config.hpp
本地示例：front_car_mainline.yaml
板端默认：/root/front_car_mainline.yaml
路径覆盖：FRONT_CAR_CONFIG
启动加载：code/app/main.cpp -> load_control_config(resolve_config_path())
每帧读取：control.cpp -> control_config()
实车闭环入口：solve_runtime_with_feedback(rt, fb, ctrl)
最终输出：drive_output_apply(ctrl)
```
