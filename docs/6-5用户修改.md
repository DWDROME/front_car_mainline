远线bug修复：

cross.cpp
关于cross_evolve，每次进入cross_evolve时都会清空：
  rt->cross.left_l = -1;
  rt->cross.right_l = -1;
然后重新进行远线识别，如果某一帧的远线能够找到，但是某种原因L并没有识别出来，则这两个数就还是-1.

可看到399行的判断条件
f(rt->cross.right_num >= k_cross_min_front_step && rt->cross.right_l >= 0) 如果

我考虑删除rt->cross.track_type = TRACK_TYPE_NONE;
但仍然无法避免这个情况

情况就是
  保留 track_type
  -> 知道该跟左/右
  -> 但 left_l/right_l 还是 -1
  -> solve_cross_mid() 不知道从远线哪里开始生成中线
  -> mid_ok = 0
  -> TRACK_REJECT_NO_MIDLINE
  -> control input invalid
  -> 停车


  对于远线的扫描应该是这个jiegou
  固定列 far_x1 = 86
  -> 从 begin_y 往上扫，找白后黑边界
  -> seed = (far_x1, far_y1 + 1)
  -> findline_lefthand_adaptive() 左手追线
  -> far_ipts0 原图远线点列
  -> mapx/mapy 透视变换
  -> far_rpts0
  -> blur
  -> far_rpts0b
  -> resample
  -> far_rpts0s
  -> local_angle + nms
  -> 扫角点
  -> far_Lpt0_rpts0s_id

为了和参考版对齐
我修改了一下
即对于left_l/right_l
      本帧找到新 L -> 覆盖
      本帧没找到新 L -> 旧值可以暂时保留
同时
left_far_found/right_far_found:
      当前帧远线点列是否构建成功
> [!note]
>     // 构建左右十字"远线"（通常为前方远 L 点串），存状态标志到 left_far_found/right_far_found
>    rt->cross.left_far_found = build_cross_farline(rt, 1);
>    rt->cross.right_far_found = build_cross_farline(rt, 0);
>    它内部有一个*far_l = -1

一个小的边界检查：
当left_l =30 但当前帧左远线只有 left_num = 20 个点
那么这个旧 id需要被覆盖
如果旧 left_l = 18，当前帧左远线 left_num = 22，后面只剩 4 个点，而最低需要 6 或 8 个点：

left_num - left_l < k_min_border_step

那也不能用。
left_pts[left_l]